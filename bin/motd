#!/bin/bash

. ~/dotfiles/bash/colors

~/dotfiles/bin/banner
~/dotfiles/bin/memusage
~/dotfiles/bin/sysinfo

echo -ne "`fgcolor2 $CLR_PRIMARY_SHADE1`"
w -i
echo -ne "`resetcolor2`"
echo

echo -ne `fgcolor2 $CLR_PRIMARY_SHADE2`
ssh-add -L | cut -d' '  -f 3 | sed "s|$HOME|~|g"
echo -ne `resetcolor2`
