#!/usr/bin/env bash

set -e

DIR="/home/ryan/projects/grryno/api-server"
FILE="api-server.tgz"
FILE_PATH="$DIR/$FILE"

cd $DIR

rm -f "$FILE_PATH"

tar --exclude="$FILE" --exclude='.[^/]*' -czf "$FILE_PATH" ./*

s3push "$FILE"
