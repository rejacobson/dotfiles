if [[ "$platform" == 'osx' ]]; then
  alias la='LC_COLLATE=C ls -alFhG'
else
  alias la='LC_COLLATE=C ls -alFh --color=always'
fi

alias lal='la | less'
alias lad="la | grep '^d'"
alias laf="la | grep -v '^d'"

alias tmux='tmux -2'

alias fuck='sudo $(history -p \!\!)'

alias vimp='vim +CtrlP'
